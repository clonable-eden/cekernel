name: kernel tests

on:
  push:
    paths:
      - 'kernel/**'
  pull_request:
    paths:
      - 'kernel/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run kernel tests
        run: bash kernel/tests/run-tests.sh
