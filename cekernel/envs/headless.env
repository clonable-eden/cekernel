# headless.env — Terminal-free execution for CI/cron/SSH
#
# Uses headless backend with higher concurrency and shorter timeout.

CEKERNEL_BACKEND=headless
CEKERNEL_MAX_WORKERS=5
CEKERNEL_WORKER_TIMEOUT=1800
