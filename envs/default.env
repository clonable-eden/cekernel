# default.env — Default profile (headless)
#
# Uses headless backend with standard concurrency.
# This profile is loaded when CEKERNEL_ENV is unset or set to "default".

CEKERNEL_BACKEND=headless
CEKERNEL_MAX_ORCH_CHILDREN=5
CEKERNEL_CI_MAX_RETRIES=3
CEKERNEL_WORKER_TIMEOUT=1800
